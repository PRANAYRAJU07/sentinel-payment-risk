[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    requires_dataset: tests that require the downloaded Kaggle dataset (deselect with -m "not requires_dataset")
